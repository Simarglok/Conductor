# Conductor — open-source data transformation platform
#
# Repo:  https://github.com/Simarglok/Conductor
# Root:  /Users/simarglok/Git/Conductor
# Stack: dbt-core 2.0.0a4 · Airflow 3.3.0 · FastAPI · PostgreSQL 18 · Redis 8.6
# Host:  macOS (Apple Silicon) · Docker Desktop
#
# ── Quick commands ──
# Start stack:        docker compose --profile init up airflow-db-init && docker compose up -d
# Stop stack:         docker compose down
# Full wipe:          docker compose down -v
# Rebuild:            docker compose build
# Logs:               docker compose logs -f <service>
# Init DB (first):    docker compose --profile init up airflow-db-init
#
# ── Services ──
# FastAPI (Conductor)    http://localhost:8000/docs
# Airflow UI (DW)        http://localhost:8081  (dw_admin / …)
# Airflow UI (Mktg)      http://localhost:8082  (mkt_admin / …)
# code-server (VS Code)  http://localhost:8443  (JWT auth via Conductor)
# PostgreSQL             localhost:5432  (conductor / conductor)
# Redis                  localhost:6379
#
# ── Dir structure ──
# backend/        — FastAPI control plane (Python 3.14, SQLAlchemy async, Alembic)
# dags/           — Airflow DAGs (per-project subdirectories)
#   data-warehouse/
#   marketing/
# docker/         — Dockerfiles (airflow/, code-server/)
# docker-compose.yml — full dev environment
# workspaces/     — code-server user data (per-user subdirectories)
# logs/           — Airflow task logs (per-project)
# frontend/       — React SPA (Vite + Tailwind + shadcn/ui)
# .hermes/        — Hermes Agent context
#   plans/          — Architecture and design docs
#   tasks/          — Implementation task files (one per agent run)
#
# ── Architecture (MVP) ──
# Auth:           JWT + bcrypt, roles/permissions DB tables
# Roles:          super_admin → project_admin → maintainer → developer → viewer (customizable)
# Airflow:        Per-project instances, proxy auth via FastAPI (role-based system accounts)
# code-server:    Multiuser v4+, JWT native auth, persistent workspaces on volume
# Git:            git CLI via subprocess, any provider (GitHub/GitLab/Bitbucket/Gitea)
# Frontend:       React SPA (Vite + Tailwind + shadcn/ui)
#
# ── Config ──
# Secrets are in .env (gitignored) at project root.
# Backend config is ENV-only via CONDUCTOR_* variables.
# Airflow config is ENV-only (no airflow.cfg).
#
# ── Milestones ──
# M1 (DONE)  Dev Sandbox — all services working
# M2 (NEXT)  Foundation DB + Auth (Phase 0 & 1 of task plan)
# M3 (TODO)  Projects + Airflow Integration (Phase 2 & 3)
# M4 (TODO)  Code-server + Git (Phase 4 & 5)
# M5 (TODO)  Frontend + Infra (Phase 6 & 7)
# M6 (DEFER) AI Assistant (Hermes Proxy)
#
# ── Task plan ──
# 29 tasks across 8 phases in .hermes/tasks/
# Phase 0: Foundation (DB models + migrations)
# Phase 1: Auth (JWT + permissions + admin API)
# Phase 2: Projects (CRUD + members + settings)
# Phase 3: Airflow (lifecycle + proxy + widgets)
# Phase 4: Code-server (JWT + workspace setup)
# Phase 5: Git (service + branch/MR API + checks)
# Phase 6: Frontend (all React pages)
# Phase 7: Infrastructure (Docker + code-server + DB)